default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  host: localhost
  username: <%= ENV["database_username"] %>
  password: <%= ENV["database_password"] %>

development:
  <<: *default
  database: voicera_development

test:
  <<: *default
  database: voicera_test